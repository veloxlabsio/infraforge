# This secret is auto-created by the Database Crossplane composition.
# For production, integrate with External Secrets Operator or Vault.
#
# The composition generates a secret named <claim-name>-creds with:
#   - password
#   - username
#   - database
#
# To override, create the secret before the claim:
#   kubectl create secret generic demo-db-creds \
#     --from-literal=password=$(openssl rand -base64 24) \
#     --from-literal=username=app \
#     --from-literal=database=app \
#     -n apps
