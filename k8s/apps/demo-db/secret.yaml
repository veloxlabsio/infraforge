# Generate this secret before applying the Database claim:
#
#   kubectl create secret generic demo-db-creds \
#     --from-literal=password=$(openssl rand -base64 24) \
#     --from-literal=username=app \
#     --from-literal=database=app \
#     -n apps
#
# For production, use External Secrets Operator or Vault instead.
# This file is documentation only — do not commit real credentials.
